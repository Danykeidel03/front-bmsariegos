# GitHub Pages configuration - Disable Jekyll completely
skip_jekyll: true

# Point to a non-existent directory to prevent Jekyll from processing
source: /_none_

# Exclude all directories from Jekyll processing
exclude:
  - "/"
